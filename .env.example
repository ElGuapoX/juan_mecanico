# Ejemplo de variables de entorno para la conexión a la base de datos
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=juan_mecanico
# Contraseña root para MySQL (usada por docker-compose si levantas el servicio db)
MYSQL_ROOT_PASSWORD=root
